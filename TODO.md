instead of putting the flag at the very end, make a custom markdown element like [[!FLAG]] that the builder will understand and where it will put the flag
when clicking on a picture/gif, make it bigger + overlay the page, clicking on the overlay (on the page but not on the picture) closes it
copy button on solve scripts
keep the "shell" logic on the paths, when we click on an event have the path be ~/event/event_name to be coherent
update about me